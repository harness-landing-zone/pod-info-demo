# Layer 3/3 — prod environment. Only what differs from defaults + prod tier.
# image.tag is written by the Harness UpdateReleaseRepo step — keep it here.
# replicaCount kept explicit: an environment declares its own capacity.
image:
  tag: v0.1
replicaCount: 3
