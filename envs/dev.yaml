# Dev (appteam-one-dev). CLASSIC Argo deployment — plain Deployment, NO rollouts.
workload:
  kind: Deployment

image:
  tag: "6.11.1"

replicaCount: 4

# (rollout/canary settings ignored for a Deployment)
analysis:
  enabled: false

monitoring:
  enabled: false

ingress:
  enabled: false
